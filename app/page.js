export default function HomePage() {
  return (
    <main className="simple-page">
      <section className="simple-panel">
        <p className="card-label">Resident treatment signups</p>
        <h1>Property signup pages are ready to configure.</h1>
        <p>
          Use a property-specific link like <strong>/p/demo-property</strong> after adding
          that property in Supabase.
        </p>
      </section>
    </main>
  );
}
